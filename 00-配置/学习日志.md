# 📝 学习日志

> 每次学习结束后，由 Codex 先整理拟写回摘要；仅在你确认后追加一条新记录。
> 记录格式见下方模板，保持历史记录完整，**不删除旧记录**。

---

## 日志模板

```markdown
### 第 X 次学习 · YYYY-MM-DD

**学习内容：** [Day X - 专题名称]

**今日完成：**
| 题号 | 题目 | 难度 | 掌握程度 | 备注 |
|------|------|------|----------|------|
| xxx | xxxxx | Easy/Medium/Hard | ✅/🔄/❌ | 核心思路/易错点 |

**掌握程度说明：** ✅ = 独立完成，🔄 = 看提示后完成，❌ = 未完成/需重做

**学习时长：** X 小时

**今日模板/框架学习：**
- [模板1名称]：掌握程度
- [模板2名称]：掌握程度

**复盘要点：**
- 本次最值得记住的判断条件/模板：
- 下次遇到同类题先检查：

**遇到的难点：**
- 难点1
- 难点2

**遗留问题（下次需要重点关注）：**
- 问题1
- 问题2

**下次复习：**
- 日期：YYYY-MM-DD
- 复习题目/知识点：

**明日计划：**
- [Day X - 专题名称]

**Codex 建议：**
- 建议1
- 建议2
```

---

## 学习记录

<!-- 以下按时间顺序追加。 -->

### 第 1 次学习 · 2026-07-06

**学习内容：** Day 9 - 单调栈与堆

**今日完成：**
| 题号 | 题目 | 难度 | 掌握程度 | 备注 |
|------|------|------|----------|------|
| 739 | 每日温度 | Medium | ✅ | 单调递减栈存下标，当前更热时用 `i - prev` 结算等待天数 |
| 84 | 柱状图中最大的矩形 | Hard | 🔄 | 单调递增栈；出栈时当前柱子是右边界，新栈顶是左边界 |
| 215 | 数组中的第 K 个最大元素 | Medium | 🔄 | 小根堆维护 k 个最大值；快选用 `target = len(nums) - k` |
| 347 | 前 K 个高频元素 | Medium | ✅ | 更推荐桶排序：频率最大不超过 n，可以把 `freq` 当桶下标 |
| 295 | 数据流的中位数 | Hard | 🔄 | 双堆维护左右两半，`small` 用存负数模拟大根堆 |

**掌握程度说明：** ✅ = 独立完成，🔄 = 看提示后完成，❌ = 未完成/需重做

**学习时长：** -- 小时

**今日模板/框架学习：**
- 单调栈：🔄，能理解 739，84 的左右边界还需要复盘。
- 堆 / Top K：✅，能区分小根堆维护 k 个最大值和桶排序场景。
- 快速选择：🔄，理解 `partition` 返回 pivot 最终下标，但需要再练手推。
- 双堆：🔄，理解中位数来自两个堆顶。

**复盘要点：**
- 单调栈保存的不是答案，而是“还没找到答案/边界的位置”。
- 84 出栈结算公式：`height = heights[mid]`，`width = i - stack[-1] - 1`。
- 215 第 k 大转成升序下标：`target = len(nums) - k`。
- 347 bucket sort 成立的原因：频率最大不会超过数组长度。

**遇到的难点：**
- 84 的左边界为什么是弹出后的新栈顶。
- 215 快选中 `store`、pivot 最终位置和搜索方向的关系。

**遗留问题（下次需要重点关注）：**
- 84 柱状图最大矩形：再手推 `[2, 1, 5, 6, 2, 3]`。
- 215 快速选择：再手写一次 `partition`。

**下次复习：**
- 日期：2026-07-07
- 复习题目/知识点：84 的宽度公式；215 的 `target` 与 `partition`

**明日计划：**
- Day 10 - 回溯算法

**Codex 建议：**
- 明天开场先用 5 分钟复盘 84 和 215，再进入回溯。
- 347 优先记桶排序版本；堆版本作为 Top K 通用模板保留。

---

### 第 2 次学习 · 2026-07-09

**学习内容：** Day 10 - 回溯算法

**今日完成：**
| 题号 | 题目 | 难度 | 掌握程度 | 备注 |
|------|------|------|----------|------|
| 46 | 全排列 | Medium | ✅ | 排列关心顺序，用 `used` 防止同一路径重复使用元素 |
| 78 | 子集 | Medium | ✅ | 子集不关心顺序，用 `start` 只往后选；每个节点都是答案 |
| 17 | 电话号码的字母组合 | Medium | ✅ | 用 `index` 逐位处理 `digits`，空输入返回 `[]` |
| 39 | 组合总和 | Medium | ✅ | 可重复选时递归传 `i`；排序后可用 `break` 剪枝 |
| 22 | 括号生成 | Medium | ✅ | `left < n` 放左括号，`right < left` 放右括号 |
| 79 | 单词搜索 | Medium | 🔄 | `dfs(i, j, k)`；访问标记只属于当前路径，递归后要还原 |
| 131 | 分割回文串 | Medium | 🔄 | `start` 是下一刀起点，`end` 是当前段终点，切片用 `s[start:end+1]` |
| 51 | N 皇后 | Hard | ✅ | 一行一层；检查 `col`、`row-col`、`row+col` 三类冲突 |

**掌握程度说明：** ✅ = 独立完成，🔄 = 看提示后完成，❌ = 未完成/需重做

**学习时长：** -- 小时

**今日模板/框架学习：**
- 回溯三要素：🟠，能区分路径、选择列表和结束条件。
- 排列 / 子集 / 组合：🟢，能判断 `used`、`start`、`index` 的使用场景。
- 网格回溯：🔄，理解访问标记和还原，但需要再手写 79。
- 棋盘回溯：🟠，理解 N 皇后的列和对角线集合。

**复盘要点：**
- 回溯核心动作：做选择 -> 递归 -> 撤销选择。
- 排列用 `used`；组合和子集用 `start`；逐位组合用 `index`；网格搜索用 `(i, j, k)`；N 皇后用 `row`。
- `path` 加入结果时要拷贝：`path[:]` 或 `path.copy()`。
- 131 中 `start` 是跨递归传递的状态，`end` 是当前层临时枚举的切割终点。

**遇到的难点：**
- 131 的 `s[start:end+1]` 和 `backtrack(end + 1)`。
- 79 为什么原地标记后必须还原。
- N 皇后为什么不用检查 `row`。

**遗留问题（下次需要重点关注）：**
- 131 分割回文串：再手写一次 `start/end` 版本。
- 79 单词搜索：分别用 `visited` 和原地标记写一次。
- 51 N 皇后：再手推 `row - col` / `row + col` 对角线。

**下次复习：**
- 日期：2026-07-10
- 复习题目/知识点：131 的切片语义；79 的访问标记还原；51 的对角线公式。

**明日计划：**
- Day 11 - 二分查找

**Codex 建议：**
- 明天开场先口述 3 个状态变量：`used`、`start`、`index` 分别对应哪类题。
- 79 和 131 各手写一遍，比继续看题解更有效。

---

> **日志创建：** 2026-07-06
